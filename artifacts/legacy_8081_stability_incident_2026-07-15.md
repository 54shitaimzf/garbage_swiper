# 8081 基线稳定性记录（2026-07-15）

## 已确认

- 8081 原启动脚本可以加载相机 `/dev/video0`、`best.engine` 和原生告警线程。
- 前台运行 45 秒未出现 Python 异常，退出码 `124` 是 `timeout 45s` 主动结束。
- 只读 `/api/status` 和 `/api/yolo/latest` 曾返回正常数据，YOLO 曾返回 `drink_white` 检测结果。
- 8082 仅监听独立 HTTP 端口，不打开相机、GPU 或 `/dev/myserial`。

## 未通过

- 旧网关常驻后台启动后曾重复退出；一次只读请求 `/api/alert/status` 发生连接重置。
- 随后车端出现 SSH 握手和 8081/8082 HTTP 响应超时，说明车端调度/推理链路处于异常高负载或阻塞状态。
- 因此不能把旧链路标记为“完整基线通过”，也不能进入 ROS2 自主接管和真实导航。

## 处理边界

本次没有修改旧 8081 网关、协议、页面、相机配置或模型；没有发运动、急停或蜂鸣器测试命令。新增功能保持在独立 `8082` mock 服务和只读 ROS2 源码范围内。
