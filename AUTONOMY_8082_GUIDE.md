# 8082 自主任务入口

车端已新增独立自主任务 PWA：

```text
http://小车当前IP:8082/
```

旧手动控制仍然使用：

```text
http://小车当前IP:8081/
```

当前车端以 `AUTONOMY_MODE=mock` 运行，页面可以验证地图选择、模式状态、停止和接口连通性，但不会发送运动指令。这样可以先在手机热点下验证新增入口，不影响旧控制链路。

地图白名单已经读取车端：

- `lab`
- `yahboomcar`

固定路线默认保持“待现场标定”，因为未知起点/目标点时自动放行 Nav2 会有碰撞风险。完成 ROS2、雷达、里程计和 TF 验证后，再把现场测量的安全航点写入 `autonomy/config/routes.json` 并启用对应路线。

本次部署确认：

- `6000` 官方控制服务仍在监听。
- `8081` 旧手动网关仍在监听。
- `8082` 新自主 PWA 正常监听。
- 8081 的状态和 YOLO 接口在 8082 启动前后均通过只读检查。
