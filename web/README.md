# 手机遥控页面

手机和 Jetson 连接同一个手机热点后，在手机浏览器访问 `http://<Jetson在手机热点中的IP>:8080/`。

页面通过 WebSocket 连接 Jetson，Jetson 再连接官方 Rosmaster TCP `127.0.0.1:6000`。网页不直接接触串口，也不绕过官方控制协议。

首次测试车轮悬空、限速 30%。松手、断线或超过 1 秒没有控制帧，网关发送零速和急停。手动 TCP 模式与 ROS2 底盘驱动不能同时占用 `/dev/myserial`。
